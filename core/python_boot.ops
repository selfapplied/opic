;;; python_boot.ops — Python bootstrap defined in opic

def parse / {text -> defs + voices}
def compose / {voices + src + dst -> result}

voice parse.ops / {ops.text -> defs + voices}
voice compose.voices / {voices + src + dst -> result}

target boot / "python.code"
voice main / {ops.file -> boot}

