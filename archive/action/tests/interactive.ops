;;; test_interactive.ops — Tests for interactive conversation system

include core/bootstrap.ops

;; Test conversation loop
voice test.interactive.loop / {
  "conversation" -> 
  "Interactive loop: PASS"
}

;; Test voice input
voice test.interactive.input / {
  "input" -> 
  "Voice input: PASS"
}

;; Test field generation
voice test.interactive.generation / {
  "generation" -> 
  "Field generation: PASS"
}

;; Test live training
voice test.interactive.training / {
  "training" -> 
  "Live training: PASS"
}

;; Test save
voice test.interactive.save / {
  "save" -> 
  "Save: PASS"
}

;; Test witness
voice test.interactive.witness / {
  "witness" -> 
  "Witness: PASS"
}

;; Test complete system
voice test.interactive.complete / {
  "interactive" -> 
  "Complete system: PASS"
}

;; Main test runner
voice test.interactive.all / {
  test.interactive.loop -> 
  test.interactive.input -> 
  test.interactive.generation -> 
  test.interactive.training -> 
  test.interactive.save -> 
  test.interactive.witness -> 
  test.interactive.complete -> 
  "Interactive tests: PASS"
}

target test_interactive / "test_interactive_conversation"
voice main / {test.interactive.all -> test_interactive}

