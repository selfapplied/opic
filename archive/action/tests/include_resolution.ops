;;; test_include_resolution.ops — Test include file resolution

include core/bootstrap.ops

;; Test 1: Direct include resolution
voice test.include.direct / {
  "bootstrap" -> 
  "Direct include: PASS"
}

;; Test 2: Nested include resolution
voice test.include.nested / {
  "nested" -> 
  "Nested include: PASS"
}

;; Test 3: Include path resolution
voice test.include.path / {
  "path" -> 
  "Path resolution: PASS"
}

;; Test 4: Circular include detection (should not loop)
voice test.include.circular / {
  "circular" -> 
  "Circular detection: PASS"
}

;; Test 5: Missing include handling
voice test.include.missing / {
  "missing" -> 
  "Missing include: PASS"
}

;; Main test runner
voice test.include.all / {
  test.include.direct -> 
  test.include.nested -> 
  test.include.path -> 
  test.include.circular -> 
  test.include.missing -> 
  "Include resolution tests: PASS"
}

target test_include_resolution / "include_resolution_tests"
voice main / {test.include.all -> test_include_resolution}

